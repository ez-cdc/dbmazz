# Rust
/target/
**/target/
**/*.rs.bk
# Note: Cargo.lock is committed for binary crates (reproducible builds).
# Only gitignore Cargo.lock in library crates.

/skills-lock.json
/Dockerfile.dev

# Byproducts
*.log
*.tmp
*.swp
*.swo
*.bak
.DS_Store

# IDEs
.idea/
.vscode/
*.iml

# Test & coverage
coverage/
*.profraw

# Claude Code
.plans/
.claude/agents/
.claude/skills/
.claude/commands/
session-logs/
skills-lock.json

# OpenSpec — local-only by policy. Specs and changes are private working
# artifacts; only CLAUDE.md and AGENTS.md should reflect AI guidance in git.
openspec/

# Internal-only docs (not part of the public surface)
/internal-docs/

# Misc
*.old

# SDD feature folders (per .claude/sdd/SPEC.md, not committed)
features/
